# Phase-05: development/CI-only tooling. Not required to run the
# orchestrator or persistence layer at runtime -- see requirements.txt
# for that. Installed by .github/workflows/ci.yml alongside
# requirements.txt.

ruff>=0.6
pytest>=7.0
pytest-cov>=5.0
